extends ./modules/main
block variables
    -let pageTitle = 'Sign Up'
block content
    .row.text-center.justify-content-center
        h1(class='text-primary mt-5') Create Account 
        form(class='signup col-11 col-sm-6 m-auto needs-validation mt-5' action="/account/signup" method='post')
            input(class='form-control', type='text', pattern="^[0-9a-zA-Z_]{3,}$", name='name', placeholder='enter your name' required)
            .invalid-feedback The field should be 3 or more character and not contain spaces or special characters 
            input(class='form-control mt-3', type='email', name='email', placeholder='enter your email' required)
            .password-container
                input(class='form-control mt-3' type='password' pattern="(?=.*\\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" name='password' placeholder='Enter a password' required)
                .invalid-feedback.main-password.w-auto
                input(class='form-control repeat-pass mt-3' type='password' placeholder='Enter the password again' required)
                .invalid-feedback.repeated-password.w-auto The password not matched
            div(class='form-check mt-3 d-flex justify-content-center align-itesm-center gap-3')
                input(class='form-check-input' id='check' type='checkbox' required)
                lable(class='form-check-lable' for='check') accept our licence
                .invalid-feedback.w-auto You must agree before submitting.
            button(class="btn btn-primary my-3", type='submit') Sign Up
        .signup-option
            span did you have account ?
            | 
            a(href='/account/login') Login
        if(error)
            p(class="alert alert-danger mt-3")= error
block js 
    script(src="/js/signup.js") 